---
'@platforma-open/milaboratories.feature-integration.ui': patch
'@platforma-open/milaboratories.feature-integration.model': patch
---

Split the QC page into two sections — "Per-sample QC" and "Raw tag-stat" — each a full-height
single-table page (the standard one-table-per-page pattern). Replaces the previous single QC page that
stacked both tables in bounded-height containers.
